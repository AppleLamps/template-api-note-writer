name: Automated Community Note Writer

on:
  workflow_dispatch:

jobs:
  run-note-writer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download and install xurl binary
        run: |
          wget -O xurl https://github.com/CommunityNotes/xurl/releases/latest/download/xurl-linux-amd64
          chmod +x xurl
          sudo mv xurl /usr/local/bin/

      - name: Run Note Writer
        run: |
          python src/main.py
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_KEY_SECRET: ${{ secrets.X_API_KEY_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
